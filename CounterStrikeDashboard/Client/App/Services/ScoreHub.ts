module csdash {
   /* 'use strict';

    export class ScoreHub {
        private _scoreHub: any;
        private _messageListeners: Array<MessageListenerFunc>;
        private _stateListeners: Array<StateListenerFunc>;

        constructor() {
            this._scoreHub = $.connection.chatHub;
            this._messageListeners = [];

            $.connection.hub.start().done(function () {
                this._stateListeners.foreach(x => x(HubStates.CONNECTED));
            });
        }
    }

    enum HubStates { CONNECTED, DISCONNECTED }

    interface MessageListenerFunc {
        (message: string): void;
    }

    interface StateListenerFunc {
        (state: string): void;
    }*/
}